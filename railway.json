{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'Starting...' >&2 && echo \"DB=$DATABASE_URL\" >&2 && alembic upgrade head 2>&1 && echo 'Alembic done' >&2 && python -m word_learn.bot 2>&1",
    "healthcheckPath": null,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
